- name: pure moved (no diff) inside a module is filtered
  input: |
    Terraform will perform the following actions:

      # terraform_data.src has moved to module.x.terraform_data.dst
        resource "terraform_data" "dst" {
            id = "abc"
        }

    Plan: 0 to add, 0 to change, 0 to destroy.
  expected: |
    Terraform will perform the following actions:

    Plan: 0 to add, 0 to change, 0 to destroy.
